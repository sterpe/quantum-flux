/** @jsx React.DOM */

var React = require('react');

var View = React.createClass({
  render: function () {
    return (
      <div id="view" className="view" >
      </div>
    );
  }
});
      

module.exports = View;
